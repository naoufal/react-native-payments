export function convertAmountToCents(amount: string): number {
    if (typeof amount !== 'string') {
        throw new Error('You must pass a string to `coverAmountToCents`');
    }

    // TODO:
    // - Finish this method
    return 9999;
}