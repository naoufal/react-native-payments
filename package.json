{
  "scripts": {
    "precommit": "lint-staged"
  },
  "devDependencies": {
    "lerna": "^2.0.0",
    "lint-staged": "^4.0.0",
    "prettier": "^1.4.4"
  },
  "lint-staged": {
    "*.js": [
      "prettier --single-quote --write",
      "git add"
    ]
  }
}
